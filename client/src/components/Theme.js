import React from "react";
import { createTheme } from "@material-ui/core";

export const theme = createTheme({
    palette:{
        primary:{
            main: '#ffab40',
            light: '#ffd180',
            dark: '#ff9100',
        },
    },
});